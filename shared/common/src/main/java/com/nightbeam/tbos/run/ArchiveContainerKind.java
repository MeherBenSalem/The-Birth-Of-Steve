package com.nightbeam.tbos.run;

/** Deterministic gameplay treatment for one valid cache marker. */
public enum ArchiveContainerKind {
    ORDINARY,
    LOCKED,
    TRAPPED,
    CURSED,
    HIDDEN,
    BOSS_REWARD
}
