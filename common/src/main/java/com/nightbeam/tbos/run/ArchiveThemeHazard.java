package com.nightbeam.tbos.run;

/** Theme hazard families placed by {@link ArchiveRoomPlacer}; server-owned, not mob AI. */
public enum ArchiveThemeHazard {
    PARALLAX_PANEL,
    LIGHT_DUST,
    COLLAPSING_TILE,
    BRITTLE_ASH,
    SHATTER_PANE,
    FALSE_SHELF,
    RESONANT_PLATE,
    INK_POOL
}
